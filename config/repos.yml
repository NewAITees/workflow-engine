# Workflow Engine Configuration

repositories:
  - name: NewAITees/workflow-engine
    poll_interval: 30         # Check every 30 seconds for testing
    work_dir: ~/.workflow-engine/workspaces
    llm_backend: codex        # Use Codex CLI
    codex_cli: codex
    claude_cli: claude
    gh_cli: gh
    log_level: INFO
