# AGENTS

## Operating rules
- Keep changes small and test-driven.
- Prefer `uv run pytest`, `uv run ruff check .`, and `uv run mypy .` before merge.
- Use labels from `config/labels.yml` consistently.
